version: 1
project: workspace-gateway
tier: strict
languages: [lua, shell]
hooks:
  pre-commit:
    - check-unstaged
    - block-sensitive-files
    - gitleaks
    - ci-check-error-swallow
    - check-banned-words
    - check-portable-shell
    - check-markdown-docs
    - check-file-length
    - check-required-hooks-present
    - check-dependency-versions
    - ci-lint
    - ci-type-check
  commit-msg:
    - check-commit-message
    - block-coauthored
  pre-push:
    - block-coauthored-history
    - ci-check-push
    - check-dead-code

overrides:
  - check-dependency-versions:
    entry: "../CI/.venv/bin/python -m ci.check_dependency_versions res/docker/docker-compose.yml"
