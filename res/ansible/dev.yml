---
# WORKSPACE-GATEWAY Ansible playbook - Podman stack lifecycle.
#
# Pattern: single playbook, tagged blocks. Makefile delegates via:
#   ansible-playbook res/ansible/dev.yml --tags <tag>
#
# INCIDENT-2026-07-07: compose build/up/down previously ran inside
#   ansible.builtin.command tasks, which captured all stdout and made
#   builds + health-probe loops look like an indefinite freeze. These
#   operations now run directly from Makefile targets (_compose-build,
#   _compose-up, _compose-down) so output streams live to the terminal.
#   This playbook handles only: health checks, ClickHouse init, model
#   sync, status, sanity.
#
# Tags:
#   start    - health checks + ClickHouse init + model sync
#   status   - podman ps + endpoint health
#   logs     - tail container logs
#   sanity   - single curl request through gateway
#
# Makefile orchestrates multi-step flows:
#   gw-start   = systemd unit start + post-start health (this playbook --tags start)
#   gw-stop    = systemd unit stop (res/ansible/compose.yml --tags stop)
#   gw-restart = drain apisix (SIGQUIT) + gw-build + systemd restart (+ health)
#   gw-test    = bash tests/run_all.sh (no ansible needed)

- name: WORKSPACE-GATEWAY dev lifecycle
  hosts: localhost
  gather_facts: true
  connection: local

  vars:
    project_root: "{{ playbook_dir | dirname | dirname }}"
    compose_file: "{{ project_root }}/res/docker/docker-compose.yml"
    venv_bin: "{{ project_root }}/.venv/bin"
    ci_boot_bin: /opt/workspace-ci/.boot-linux/bin
    compose_cmd: "{{ venv_bin }}/podman-compose -f {{ compose_file }}"
    podman_path: "{{ ci_boot_bin }}/podman"
    admin_api_key: "{{ lookup('env', 'ADMIN_KEY') }}"
    ansible_cache_dir: "{{ lookup('env', 'HOME') }}/.cache/gateway-ansible"
    run_log: "{{ lookup('env', 'HOME') }}/.cache/gateway-ansible/dev-{{ ansible_date_time.epoch }}.log"

  environment:
    PATH: "{{ ci_boot_bin }}:{{ venv_bin }}:{{ lookup('env', 'PATH') }}"
    SHG_SCRIPT_PATH: "{{ ci_boot_bin }}/podman"

  tasks:

    # ── always: resolve paths, verify prerequisites ──────────────────────
    - name: Resolve project root
      ansible.builtin.set_fact:
        project_root: "{{ project_root }}"
        compose_file: "{{ compose_file }}"
        compose_cmd: "{{ compose_cmd }}"
      tags: always

    - name: Create per-run Ansible log directory
      ansible.builtin.file:
        path: "{{ ansible_cache_dir }}"
        state: directory
        mode: '0700'
      tags: always

    - name: Verify podman is available
      ansible.builtin.shell: command -v podman >>{{ run_log | quote }} 2>&1
      register: podman_check
      changed_when: false
      tags: always

    - name: Show podman path
      ansible.builtin.debug:
        msg: "podman: {{ podman_check.stdout }}"
      tags: always

    - name: Verify podman-compose is available
      ansible.builtin.stat:
        path: "{{ venv_bin }}/podman-compose"
      register: compose_bin
      failed_when: not compose_bin.stat.exists
      tags: always

    - name: Require ADMIN_KEY environment variable
      ansible.builtin.assert:
        that:
          - lookup('env', 'ADMIN_KEY') | length > 0
        fail_msg: "ADMIN_KEY must be set (add to .env, see .env.example)"
      tags: always


# ── start: render apisix.yaml, health checks + init + sync ──────────
# NOTE: compose build/up/down are now Makefile targets (see header).
# This tag assumes the stack is already up via the systemd unit.
# APISIX standalone YAML mode polls apisix.yaml every 1s, so rendering
# the .j2 here (after compose-up) hot-reloads the llamafile route into
# the running APISIX without a container restart.
# Subtags: setup (one-time init tasks), health (service readiness checks).
# Both run under --tags start; --tags health or --tags setup selects subset.
    - name: Capture llamafile upstream env vars (empty -> .j2 defaults apply)
      ansible.builtin.set_fact:
        LLAMAFILE_UPSTREAM_HOST: "{{ lookup('env', 'LLAMAFILE_UPSTREAM_HOST') }}"
        LLAMAFILE_UPSTREAM_PORT: "{{ lookup('env', 'LLAMAFILE_UPSTREAM_PORT') }}"
      tags: [start, setup]

    - name: Render conf/apisix.yaml from .j2 template (env-driven llamafile route)
      ansible.builtin.template:
        src: "{{ project_root }}/conf/apisix.yaml.j2"
        dest: "{{ project_root }}/conf/apisix.yaml"
        mode: '0644'
        backup: false
      tags: [start, setup]

    - name: Wait for OpenBao to be reachable and unsealed
      ansible.builtin.uri:
        url: http://localhost:8201/v1/sys/health
        method: GET
        # OpenBao returns 200 when active, 429 when standby, and 503 while
        # sealed or still starting. Only 200/429 mean the API is usable.
        status_code: [200, 429, 501, 503]
        timeout: 5
      register: bao_health
      until: bao_health.status in [200, 429]
      retries: 30
      delay: 2
      tags: [start, health]

    - name: Wait for APISIX to be ready
      ansible.builtin.uri:
        url: http://localhost:9080/
        method: GET
        status_code: [200, 404]
        timeout: 5
      register: apisix_health
      until: apisix_health.status in [200, 404]
      retries: 15
      delay: 2
      tags: [start, health]

    - name: Wait for etcd to be ready
      ansible.builtin.uri:
        url: http://localhost:2379/health
        method: GET
        status_code: [200]
        timeout: 5
      register: etcd_health
      until: etcd_health.status == 200
      retries: 15
      delay: 2
      tags: [start, health]

    - name: Wait for APISIX Admin API to be ready
      ansible.builtin.uri:
        url: http://localhost:9180/apisix/admin/routes
        method: GET
        headers:
          X-API-KEY: "{{ admin_api_key }}"
        status_code: [200]
        timeout: 5
      register: admin_health
      until: admin_health.status == 200
      retries: 15
      delay: 2
      tags: [start, health]

    - name: Verify required APISIX plugins are loaded
      ansible.builtin.uri:
        url: http://localhost:9180/apisix/admin/plugins/list
        method: GET
        headers:
          X-API-KEY: "{{ admin_api_key }}"
        status_code: [200]
        timeout: 5
      register: apisix_plugins
      vars:
        required_custom_plugins:
          - key-resolver
          - key-meta
          - kimi-auth
          - openai-auth
          - provider-sync
          - redact
          - sse-usage
      failed_when: (required_custom_plugins | difference(apisix_plugins.json | default([]))) | length > 0
      tags: [start, health, status]

    - name: Seed routes from conf/apisix.yaml into Admin API (etcd)
      block:
        - name: Create temporary admin key file
          ansible.builtin.tempfile:
            state: file
            prefix: gateway-admin-key-
          register: admin_key_file
          no_log: true

        - name: Write admin key to temporary file
          ansible.builtin.copy:
            content: "{{ admin_api_key }}\n"
            dest: "{{ admin_key_file.path }}"
            mode: '0600'
          no_log: true

        - name: Reconcile routes
          ansible.builtin.shell: >
            {{ project_root }}/res/scripts/seed-routes.sh --admin-key-file {{ admin_key_file.path | quote }}
            >>{{ run_log | quote }} 2>&1
          args:
            chdir: "{{ project_root }}"
          no_log: true
          register: seed_result
          changed_when: "'seeded' in (seed_result.stdout | default(''))"

        - name: Show seed result
          ansible.builtin.debug:
            var: seed_result.stdout_lines
      always:
        - name: Remove temporary admin key file
          ansible.builtin.file:
            path: "{{ admin_key_file.path | default('') }}"
            state: absent
          when: admin_key_file.path is defined
          no_log: true
      tags: [start, setup]

    - name: Wait for ClickHouse to be ready
      ansible.builtin.uri:
        url: http://localhost:8123/ping
        method: GET
        status_code: [200]
        timeout: 5
      register: ch_health
      until: ch_health.status == 200
      retries: 15
      delay: 2
      tags: [start, health]

    - name: Wait for Vector ingest endpoint to be reachable
      ansible.builtin.uri:
        url: http://localhost:18080/ingest
        method: GET
        status_code: [200, 404, 405]
        timeout: 5
      register: vector_health
      until: vector_health.status in [200, 404, 405]
      retries: 15
      delay: 2
      tags: [start, health]

    - name: Run ClickHouse init SQL (idempotent)
      ansible.builtin.shell: >
        podman exec -i docker_clickhouse_1 clickhouse-client --multiquery
        < {{ project_root }}/conf/clickhouse-init.sql
        >>{{ run_log | quote }} 2>&1
      register: ch_init
      changed_when: true
      tags: [start, setup]

    - name: Show ClickHouse init output
      ansible.builtin.debug:
        var: ch_init.stdout_lines
      tags: [start, setup]

    - name: Run ClickHouse schema migrations (golang-migrate via compose)
      ansible.builtin.shell: >
        {{ compose_cmd }} --profile migration run --rm migrate up
        >>{{ run_log | quote }} 2>&1
      args:
        chdir: "{{ project_root }}"
      register: ch_migrate
      changed_when: "'no change' not in (ch_migrate.stdout | default(''))"
      tags: [start, setup]

    - name: Show schema migration output
      ansible.builtin.debug:
        var: ch_migrate.stdout_lines
      tags: [start, setup]

    - name: Wait for Prometheus metrics endpoint
      ansible.builtin.uri:
        url: http://localhost:9100/apisix/prometheus/metrics
        method: GET
        status_code: [200]
        timeout: 5
      register: prom_health
      until: prom_health.status == 200
      retries: 15
      delay: 2
      tags: [start, health]

    - name: Wait for gateway Prometheus to be ready
      ansible.builtin.uri:
        url: http://localhost:9092/-/healthy
        method: GET
        status_code: [200]
        timeout: 5
      register: gw_prom_health
      until: gw_prom_health.status == 200
      retries: 15
      delay: 2
      tags: [start, health]

    - name: Wait for Grafana to be ready
      ansible.builtin.uri:
        url: http://localhost:3030/api/health
        method: GET
        status_code: [200]
        timeout: 5
      register: grafana_health
      until: grafana_health.status == 200
      retries: 30
      delay: 3
      tags: [start, health]

    - name: Display stack endpoints
      ansible.builtin.debug:
        msg: |
          Stack is ready.
            APISIX:             http://localhost:9080
            APISIX Admin API:   http://localhost:9180
            APISIX Dashboard:   http://localhost:9180/ui/
            Grafana:            http://localhost:3030
            Prometheus:         http://localhost:9092
            APISIX metrics:     http://localhost:9100/apisix/prometheus/metrics
            ClickHouse:         http://localhost:8123
            Vector:             http://localhost:18080/ingest
            OpenBao:            http://localhost:8201
            etcd:               http://localhost:2379
      tags: [start, health]

    - name: Trigger provider model sync on the gateway
      ansible.builtin.shell: |
        if [ -f {{ project_root }}/.env ]; then
          set -a; . {{ project_root }}/.env; set +a
        fi
         curl -sS -f --max-time 30 -X POST http://localhost:9080/gateway/providers/sync >>{{ run_log | quote }} 2>&1
      args:
        chdir: "{{ project_root }}"
      register: sync_result
      changed_when: true
      tags: [start, setup]

    - name: Show sync result
      ansible.builtin.debug:
        var: sync_result.stdout_lines
      tags: [start, setup]


    # ── status: show running containers + health ─────────────────────────
    - name: Show running containers
      ansible.builtin.shell: >
        {{ podman_path }} ps --format "table {{ '{{' }}.Names{{ '}}' }}\t{{ '{{' }}.Status{{ '}}' }}\t{{ '{{' }}.Ports{{ '}}' }}"
        >>{{ run_log | quote }} 2>&1
      register: ps_output
      changed_when: false
      tags: [status, never]

    - name: Display running containers
      ansible.builtin.debug:
        var: ps_output.stdout_lines
      tags: [status, never]

    - name: Check APISIX health
      ansible.builtin.uri:
        url: http://localhost:9080/
        method: GET
        status_code: [200, 404]
        timeout: 5
      register: apisix_status
      failed_when: apisix_status.status | default(0) < 0
      tags: [status, never]

    - name: Check ClickHouse health
      ansible.builtin.uri:
        url: http://localhost:8123/ping
        method: GET
        status_code: [200]
        timeout: 5
      register: ch_status
      failed_when: ch_status.status | default(0) < 0
      tags: [status, never]

    - name: Check etcd health
      ansible.builtin.uri:
        url: http://localhost:2379/health
        method: GET
        status_code: [200]
        timeout: 5
      register: etcd_status
      failed_when: etcd_status.status | default(0) < 0
      tags: [status, never]

    - name: Check APISIX Admin API
      ansible.builtin.uri:
        url: http://localhost:9180/apisix/admin/routes
        method: GET
        headers:
          X-API-KEY: "{{ admin_api_key }}"
        status_code: [200]
        timeout: 5
      register: admin_status
      failed_when: admin_status.status | default(0) < 0
      tags: [status, never]

    - name: Check required APISIX plugins
      ansible.builtin.uri:
        url: http://localhost:9180/apisix/admin/plugins/list
        method: GET
        headers:
          X-API-KEY: "{{ admin_api_key }}"
        status_code: [200]
        timeout: 5
      register: apisix_plugins_status
      vars:
        required_custom_plugins:
          - key-resolver
          - key-meta
          - kimi-auth
          - openai-auth
          - provider-sync
          - redact
          - sse-usage
      failed_when: (required_custom_plugins | difference(apisix_plugins_status.json | default([]))) | length > 0
      tags: [status, never]

    - name: Check Prometheus metrics
      ansible.builtin.uri:
        url: http://localhost:9100/apisix/prometheus/metrics
        method: GET
        status_code: [200]
        timeout: 5
      register: prom_status
      failed_when: prom_status.status | default(0) < 0
      tags: [status, never]

    - name: Check Grafana health
      ansible.builtin.uri:
        url: http://localhost:3030/api/health
        method: GET
        status_code: [200]
        timeout: 5
      register: grafana_status
      failed_when: grafana_status.status | default(0) < 0
      tags: [status, never]

    - name: Check gateway Prometheus health
      ansible.builtin.uri:
        url: http://localhost:9092/-/healthy
        method: GET
        status_code: [200]
        timeout: 5
      register: gw_prom_status
      failed_when: gw_prom_status.status | default(0) < 0
      tags: [status, never]

    - name: Display health status
      ansible.builtin.debug:
        msg:
          - "APISIX:           {{ 'UP' if apisix_status.status in [200, 404] else 'DOWN' }}"
          - "APISIX Admin:     {{ 'UP' if admin_status.status == 200 else 'DOWN' }}"
          - "etcd:             {{ 'UP' if etcd_status.status == 200 else 'DOWN' }}"
          - "ClickHouse:       {{ 'UP' if ch_status.status == 200 else 'DOWN' }}"
          - "APISIX metrics:   {{ 'UP' if prom_status.status == 200 else 'DOWN' }}"
          - "Grafana:          {{ 'UP' if grafana_status.status == 200 else 'DOWN' }}"
          - "Prometheus:       {{ 'UP' if gw_prom_status.status == 200 else 'DOWN' }}"
      tags: [status, never]


    # ── sanity: single request through gateway ───────────────────────────
    - name: Send sanity check request
      ansible.builtin.shell: |
        if [ -f {{ project_root }}/.env ]; then
          set -a; . {{ project_root }}/.env; set +a
        fi
        curl -s -o /dev/null -w "%{http_code}" --max-time 30 \
          -X POST http://localhost:9080/opencode/v1/chat/completions \
          -H "Authorization: Bearer $OPENCODE_API_KEY" \
          -H "Content-Type: application/json" \
          -d '{"model":"minimax-m3","messages":[{"role":"user","content":"Say hello"}],"stream":false}' \
          >>{{ run_log | quote }} 2>&1
      args:
        chdir: "{{ project_root }}"
      register: sanity_code
      changed_when: false
      failed_when: sanity_code.rc != 0 or (sanity_code.stdout | trim) not in ['200', '401']
      tags: [sanity, never]

    - name: Show sanity result
      ansible.builtin.debug:
        msg: "Sanity check HTTP status: {{ sanity_code.stdout }}"
      tags: [sanity, never]
