# WORKSPACE-GATEWAY - Managed services declaration
# Read by workspace/cli/status_systemd.py for orphan detection.

compose_services:
  gateway-compose:
    compose_file: res/docker/docker-compose.yml

podman_pods:
  gateway:
    containers:
      - docker_apisix_1
      - docker_clickhouse_1
      - docker_vector_1
      - gw-etcd
      - gw-grafana
      - gw-prometheus
      - gw-openbao
