{
  "name": "gateway-treemap",
  "version": "1.0.0",
  "private": true,
  "description": "Grafana panel: templated treemap with min/max tile-area constraints",
  "license": "Apache-2.0",
  "scripts": {
    "build": "bash build.sh",
    "test": "node --test test/"
  }
}
