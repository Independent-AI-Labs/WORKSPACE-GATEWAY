# AUTO-GENERATED by /opt/workspace-ci/scripts/scaffold-ci [2026-08-27T13:08:30Z]
# Source: ci-profile.yaml | tier=strict | languages=lua shell
# Re-generate (safe, pre-commit only): make -C /opt/workspace-ci scaffold-ci CONSUMER=. --force-precommit --yes
# WARNING: --force-all overwrites Makefile + config/*.
#          --force is removed; use --force-precommit | --force-makefile | --force-configs | --force-all.
repos:
  - repo: local
    hooks:
      - id: check-unstaged
        name: "Check Unstaged"
        entry: "bash -c 'source /opt/workspace-ci/lib/checks.sh && ci_check_unstaged'"
        language: system
        pass_filenames: false
        always_run: true
      - id: block-sensitive-files
        name: "Block Sensitive Files"
        entry: "bash -c 'source /opt/workspace-ci/lib/checks.sh && ci_block_sensitive_files'"
        language: system
        pass_filenames: true
        always_run: false
      - id: gitleaks
        name: "Gitleaks"
        entry: "bash -c 'source /opt/workspace-ci/lib/checks.sh && ci_scan_secrets'"
        language: system
        pass_filenames: false
        always_run: true
      - id: ci-check-error-swallow
        name: "Ci Check Error Swallow"
        entry: "bash -c 'source /opt/workspace-ci/lib/checks.sh && ci_check_silent_swallow'"
        language: system
        pass_filenames: false
        always_run: true
      - id: check-banned-words
        name: "Check Banned Words"
        entry: "bash -c 'source /opt/workspace-ci/lib/checks.sh && ci_check_banned_words'"
        language: system
        pass_filenames: false
        always_run: false
      - id: check-portable-shell
        name: "Check Portable Shell"
        entry: "bash -c 'source /opt/workspace-ci/lib/checks.sh && ci_check_portable_shell'"
        language: system
        pass_filenames: false
        always_run: true
      - id: check-markdown-docs
        name: "Check Markdown Docs"
        entry: "uv run --project /opt/workspace-ci --no-sync python -m ci.check_markdown_docs --all-md --check-remote"
        language: system
        pass_filenames: true
        always_run: false
      - id: check-file-length
        name: "Check File Length"
        entry: "bash -c 'source /opt/workspace-ci/lib/checks.sh && ci_check_file_length'"
        language: system
        pass_filenames: false
        always_run: true
      - id: check-required-hooks-present
        name: "Check Required Hooks Present"
        entry: "uv run --project /opt/workspace-ci --no-sync python -m ci.check_required_hooks_present"
        language: system
        pass_filenames: false
        always_run: true
      - id: check-dependency-versions
        name: "Check Dependency Versions"
        entry: "uv run --project /opt/workspace-ci --no-sync python -m ci.check_dependency_versions validate --consumer-root "$PWD" --exclusions config/dependency_excludes.yaml --catalog-if-present res/dependency-pins.yaml --compose res/docker/docker-compose.yml"
        language: system
        pass_filenames: false
        always_run: true
      - id: ci-lint
        name: "Ci Lint"
        entry: "make lint"
        language: system
        pass_filenames: false
        always_run: true
      - id: ci-type-check
        name: "Ci Type Check"
        entry: "make type-check"
        language: system
        pass_filenames: false
        always_run: true
      - id: check-module-size
        name: "Check Module Size"
        entry: "bash -c 'source /opt/workspace-ci/lib/checks.sh && ci_check_module_size'"
        language: system
        pass_filenames: false
        always_run: true
      - id: check-npm-lock-sync
        name: "Check Npm Lock Sync"
        entry: "uv run --project /opt/workspace-ci --no-sync python -m ci.check_npm_lock_sync"
        language: system
        pass_filenames: false
        always_run: true
      - id: check-coverage-no-devolution
        name: "Check Coverage No Devolution"
        entry: "bash -c 'source /opt/workspace-ci/lib/checks.sh && ci_check_coverage_thresholds_no_devolution'"
        language: system
        pass_filenames: false
        always_run: true
      - id: check-resolution-shapes
        name: "Check Resolution Shapes"
        entry: "bash -c 'source /opt/workspace-ci/lib/checks.sh && ci_check_fallback_resolution'"
        language: system
        pass_filenames: false
        always_run: false
      - id: check-commit-message
        name: "Check Commit Message"
        entry: "bash -c 'source /opt/workspace-ci/lib/checks.sh && ci_check_commit_message "$1"' --"
        language: system
        pass_filenames: false
        always_run: true
        stages: [commit-msg]
      - id: block-coauthored
        name: "Block Coauthored"
        entry: "bash -c 'source /opt/workspace-ci/lib/checks.sh && ci_block_coauthored "$1"' --"
        language: system
        pass_filenames: false
        always_run: true
        stages: [commit-msg]
      - id: block-coauthored-history
        name: "Block Coauthored History"
        entry: "bash -c 'source /opt/workspace-ci/lib/checks.sh && ci_block_coauthored_history'"
        language: system
        pass_filenames: false
        always_run: true
        stages: [pre-push]
      - id: ci-check-push
        name: "Ci Check Push"
        entry: "make check-push"
        language: system
        pass_filenames: false
        always_run: true
        stages: [pre-push]
      - id: check-dead-code
        name: "Check Dead Code"
        entry: "bash -c 'source /opt/workspace-ci/lib/checks.sh && ci_check_dead_code'"
        language: system
        pass_filenames: false
        always_run: true
        stages: [pre-push]
